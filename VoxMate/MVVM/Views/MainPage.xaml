<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VoxMate.MVVM.Views.MainPage"
             BackgroundColor="{DynamicResource PageBackgroundColor}"
             Padding="20">

    <ContentPage.Resources>
        <!-- Estilos locales que reutilizan recursos globales -->
        <Style TargetType="Frame" x:Key="CardFrame">
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="HasShadow" Value="True" />
            <Setter Property="BackgroundColor" Value="{DynamicResource CardBackground}" />
        </Style>

        <Style TargetType="Label" x:Key="Title">
            <Setter Property="FontSize" Value="30" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="TextColor" Value="{DynamicResource PrimaryTextColor}" />
        </Style>

        <Style TargetType="Label" x:Key="Subtitle">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="TextColor" Value="{DynamicResource SectionTitleColor}" />
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="14,12" />
            <Setter Property="BackgroundColor" Value="{DynamicResource AccentColor}" />
            <Setter Property="TextColor" Value="White" />
        </Style>

        <Style TargetType="Label" x:Key="FeatureText">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="TextColor" Value="{DynamicResource PrimaryTextColor}" />
        </Style>
    </ContentPage.Resources>

    <Grid RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <HorizontalStackLayout Grid.Row="0" VerticalOptions="Center" Spacing="12">
            <Label Text="🎙️" FontSize="28" VerticalOptions="Center" />
            <VerticalStackLayout>
                <Label Text="VoxMate" Style="{StaticResource Title}" />
                <Label Text="Asistente de voz — transcribe y responde" Style="{StaticResource Subtitle}" />
            </VerticalStackLayout>
        </HorizontalStackLayout>

        <!-- Content -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="16">
                <Frame Style="{StaticResource CardFrame}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="¿Qué puede hacer VoxMate?" FontAttributes="Bold" TextColor="{DynamicResource SectionTitleColor}" />
                        <StackLayout Spacing="10">
                            <HorizontalStackLayout Spacing="10">
                                <Label Text="•" TextColor="{DynamicResource AccentColor}" />
                                <Label Text="Transcripción en directo mientras hablas" Style="{StaticResource FeatureText}" />
                            </HorizontalStackLayout>
                            <HorizontalStackLayout Spacing="10">
                                <Label Text="•" TextColor="{DynamicResource AccentColor}" />
                                <Label Text="Procesa comandos al pulsar «Detener»" Style="{StaticResource FeatureText}" />
                            </HorizontalStackLayout>
                            <HorizontalStackLayout Spacing="10">
                                <Label Text="•" TextColor="{DynamicResource AccentColor}" />
                                <Label Text="Cálculos, conversiones y consultas rápidas" Style="{StaticResource FeatureText}" />
                            </HorizontalStackLayout>
                            <HorizontalStackLayout Spacing="10">
                                <Label Text="•" TextColor="{DynamicResource AccentColor}" />
                                <Label Text="Reproduce la respuesta por altavoz (TTS)" Style="{StaticResource FeatureText}" />
                            </HorizontalStackLayout>
                        </StackLayout>
                    </VerticalStackLayout>
                </Frame>

                <Frame Style="{StaticResource CardFrame}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="Acciones rápidas" FontAttributes="Bold" TextColor="{DynamicResource SectionTitleColor}" />
                        <HorizontalStackLayout Spacing="12" HorizontalOptions="Center">
                            <Button Text="Ir al asistente" Style="{StaticResource PrimaryButton}" Command="{Binding StartCommand}" />
                            <Button Text="Historial" BackgroundColor="Transparent" TextColor="{DynamicResource SectionTitleColor}"
                                    Clicked="OnOpenHistoryClicked" />
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Frame>
            </VerticalStackLayout>
        </ScrollView>

        <!-- Footer: hint con gesto -->
        <HorizontalStackLayout Grid.Row="2" HorizontalOptions="Center" VerticalOptions="Center" Spacing="6">
            <Label Text="También puedes deslizar →" TextColor="{DynamicResource SectionTitleColor}" />
            <Label Text="(o usar el botón)" TextColor="{DynamicResource SectionTitleColor}" />
        </HorizontalStackLayout>

        <!-- Swipe Recognizer: dispara el mismo OnSwipe del code-behind -->
        <Grid.GestureRecognizers>
            <SwipeGestureRecognizer Direction="Right" Swiped="OnSwipe" />
        </Grid.GestureRecognizers>
    </Grid>
</ContentPage>